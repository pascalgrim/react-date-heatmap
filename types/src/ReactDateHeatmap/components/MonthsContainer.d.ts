import { MonthEntry } from '../types';
type MonthsContainerProps = {
    months: MonthEntry[];
    squareWidth: number;
    textColor: string;
};
declare function MonthsContainer({ months, squareWidth, textColor }: MonthsContainerProps): import("react/jsx-runtime").JSX.Element;
export default MonthsContainer;
